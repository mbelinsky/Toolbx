<ul class="items">
	<li class="link">
		<%= link_to 'Add Tool', new_admin_tool_path %>
	</li>

	<% @tools.each do |tool| %>
		<%= render 'tools/tool', tool: tool, path: edit_admin_tool_path(tool) %>
	<% end %>
</ul>